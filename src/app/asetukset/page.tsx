'use client';
import Settings from "./Settings";

export default function page() {
    return (
        <main className="flex min-h-screen flex-col p-2 gap-8">
            <Settings />
        </main>
    );
}